<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>gallOURy</title>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
    rel="stylesheet"/>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <script
    type="module"
    src="https://cdn.jsdelivr.net/npm/@bufferhead/nightowl@0.0.14/dist/nightowl.js"></script>
    <script type="module">
    import { createNightowl } from '@bufferhead/nightowl'

    createNightowl({
        defaultMode: 'dark',
        toggleButtonMode: 'newState'
    })
</script>
</head>

<body>

<% if(nav === true){ %>
    <div class="w-full px-10 py-4 flex justify-between bg-red-50 text-zinc-600 items-center">
        <a href="/feed"><h3 class="flex gap-10 text-3xl font-semibold">gallOURy</h3></a>
        <div class="flex gap-10 items-center font-semibold px-[70px]">
            <a class="hover:underline focus:underline" href="/profile">Profile</a><a class="hover:underline focus:underline" href="/feed">Feed</a><a class="px-3 py-2 bg-red-600 text-white rounded-full hover:bg-red-700 focus:bg-red-700" href="/logout">Logout</a>
        </div>
    </div>
<% } %>